{
  "name": "portal-casas",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @portal/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "postinstall": "echo 'No postinstall needed'"
  },
  "pnpm": {
      "overrides": {
        "drizzle-orm": "0.45.2"
      }
    }
}
