{
  "name": "portal-casas",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @portal/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "postinstall": "pnpm --filter @portal/db exec prisma generate"
  }
}
