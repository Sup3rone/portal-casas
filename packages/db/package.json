{
  "name": "@portal/db",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "seed": "tsx --env-file=.env prisma/seed.ts"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "prisma": "^6.0.0",
    "tsx": "^4.0.0"
  }
}
