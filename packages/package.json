{
  "name": "portal-casas",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "pnpm --filter @portal/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
